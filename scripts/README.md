# Scripts

Scripts utilitários para automações da organização (ex.: rulesets/branch protection).

## Requisitos
- `gh` (GitHub CLI)
- Autenticação via `gh auth login` **ou** `GH_TOKEN` exportado no ambiente
