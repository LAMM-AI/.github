# data/

Esta pasta **não deve conter dados versionados**.

Coloque aqui apenas instruções e (opcionalmente) scripts de download/preprocessamento.

Sugestão:

- `scripts/download_data.sh` (ou `.py`)
- `data/MANIFEST.json` com checksums/IDs das versões usadas
